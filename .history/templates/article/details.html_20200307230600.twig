{% extends 'base.html.twig' %}

{% block body %}
	<div class="row">
		<div class="col-sm-3">
			<img src="{{ asset('img/product/' ~ product.image) }}" class="img-fluid" alt="">
		</div>
		<div class="col-sm-9">
			<h3>{{ article.name }}</h3>
			<p>{{ article.description }}</p>

			<hr>
			<p class="h6">
				<span class="font-weight-bold">Cat̩gorie :</span>
				{{ product.continent.name }}</p>
		</div>
	</div>
{% endblock %}
